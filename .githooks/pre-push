#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

echo "[pre-push] Running app local CI checks (npm run -w app test:ci:local)..."
npm run -w app test:ci:local
echo "[pre-push] Local CI checks passed."
